import { retiredStorageRoute } from '@/lib/storage/retired-route';
export const dynamic = 'force-dynamic';
export {
  retiredStorageRoute as GET,
  retiredStorageRoute as POST,
  retiredStorageRoute as PUT,
  retiredStorageRoute as PATCH,
  retiredStorageRoute as DELETE,
  retiredStorageRoute as HEAD,
  retiredStorageRoute as OPTIONS,
};
