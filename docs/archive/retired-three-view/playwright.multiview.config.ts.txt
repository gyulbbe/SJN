import { defineConfig } from '@playwright/test';

export default defineConfig({
  testDir: './e2e',
  testMatch: 'multiview.spec.ts',
  outputDir: './test-results/multiview-browser',
  fullyParallel: false,
  workers: 1,
  timeout: 1_800_000,
  expect: { timeout: 20_000 },
  reporter: 'line',
  webServer: {
    command: 'npm run dev',
    url: 'http://127.0.0.1:3000',
    reuseExistingServer: !process.env.CI,
    timeout: 120_000,
  },
});
