# v10 실제 4장 재검증 중간 결과

2026-09-16. 전체 품질 목표는 미완료다. 현재 소스의 분류 충돌 처리와 샤워 모형 확장은 구현·빌드·좁은 실제 검증을 마쳤지만 15장 최종 채택 검증은 아직 하지 않았다.

## 이번 변경

- `classification-resolution.ts`: 원 설비 계열을 다른 종류로 바꾸려면 별도의 직접 관측과 해당 구조 근거를 요구한다. 충돌하면 원 후보를 확인 필요한 추정 가설로 유지한다. 원응답·독립 확인·사용자 수정·중복 제외 근거는 보존한다.
- `quality-core.ts`: Cloud 경로에서 분류 충돌을 정리한 뒤 샤워 상세·설치 상태·배치 관계를 분석한다. 기존 로컬 분석 순서는 유지한다. 수정된 후보에 이전 확인서를 임의로 재사용하지 않는다.
- `shower-model-observation.ts`, `fixture-variants.ts`, `templates.ts`: 레일 없는 벽걸이 헤드·호스·조작부 및 고정 헤드·연결관 모형을 추가한다. 관측 부위와 전체 장치의 범위는 분리하고 규격은 기본/추정값으로 표시한다.
- 시안·Before/After·원본·기존 자재를 덮어쓰지 않는다. 새 모델·런타임·운영 배포·커밋은 없다.

## 실제 좁은 검증

실행: `test-results/reconstruction-fifteen-rebuild/2026-09-16T02-41-56-428Z-cloud-gemma/narrow-ui-v10-01`.
소스 SHA: `30a341e1632d26225f2cd39b96e1d96bdbb50e4d4c79fa1bef0199d0046de17e`.
Chrome 실제 UI에서 4장 완료. 신규 binding 호출 29회, 재사용 단계 4개, 기록된 입력 44,766 / 출력 5,824 tokens. 원본·원응답·후보·모형·PNG 연결을 보존했다. 사진마다 upstream 응답이 달라진 부분이 있으므로 모든 차이를 이번 코드의 효과로 보지 않는다.

| 사진 | 개선 | 남은 핵심 실패 | 판정 |
| --- | --- | --- | --- |
| remote-01 | 소형 스프레이가 일반 벽걸이 샤워 장치로 바뀜 | 잘못된 above 관계를 따라 수전 선반이 헤드 위로 이동, 작은 창 과대 | 실패 |
| remote-02 | 실제 창의 뒤벽 배치 개선 | 새 inventory부터 샤워 누락, 세면대·거울의 벽과 변기 관계 오류 | 실패·일부 악화 |
| remote-04 | 잘못된 거울 대체를 바로잡아 유리 복구 | 낮은 벽 누락, 상부 헤드 모형/둥근 거울·볼 형상 누락 | 실패 |
| additional-05 | 샤워 복구, 가짜 두 번째 유리 제거 | 긴 뒤벽 턱이 짧은 우측 선반, 레일 관측 충돌 | 실패 |

네 장 모두 고정한 전체 핵심 품질 기준 통과는 아니다. API 성공·모형 수 증가·충돌 검사 통과를 품질 통과로 취급하지 않는다. 모바일 비교 확대판은 각 전후 이미지에서 동일한 방 전체 범위를 사용하고 full-frame 원본 출력도 보존했다.

## 검증 구분

- 전체 단위 201파일/3,099개 통과(마지막 분류 원자 복원 보정 전); 마지막 보정 이후 해당 3파일/173개 통과.
- typecheck, lint, Next build, vinext build 통과.
- 새 샤워 2종을 포함한 6개 모형의 실제 WebGL/설정 브라우저 검증 통과. 이는 표준 모형 검사이며 실제 사진 인식 성공 집계가 아니다.
- v9 일반 UI에서 Before 생성·빈 After·After 편집 후 재분석 보존·undo/redo·저장/재진입 확인. 별도 기존 Playwright headless_shell에서 90도 4회 회전 저장·복원과 4096px PNG 출력 확인. OS Chrome의 PNG 다운로드 프로세스 충돌은 별도 환경 이슈로 남겼고 해당 실행을 성공으로 바꾸지 않았다.
- v10 변경 후 일반 UI 전체 회귀와 최종 15장 실행은 아직 미실행이다.

## 비용·중단 조건

사용자 지시에 따라 Cloudflare 무료 일일 한도 도달 시 추가 추론을 멈추고 완료된 결과·부분 단계·미완료 항목을 정리한다. 한도 소진 후 자동 재시도·유료 대체·다음 날 자동 재개는 하지 않는다.

2026-09-16 08:28:44 UTC 계정 GraphQL 집계: 추론 442회, 8,047.727 / 10,000 Neurons. Adaptive analytics의 당시 값이며 즉시 실시간 잔여 보증이 아니다. 전체 계정 집계와 특정 실행 로그는 분리한다. 공식 요금표의 토큰 환산으로 v9 정상 15장/100회는 약 1,876.98N, 장당 평균 약125.13N이다. 그 조건이면 하루 약80장 규모이나 관측 범위와 재시도·캐시에 따라 크게 달라진다. 실제 계정 과금/한도 판정은 Cloudflare를 따른다.

## 다음 확인

사진 방향 관계의 영역이 겹쳐 검증 불가능한데도 큰 관계 점수로 반영되는 문제, 샤워의 빈 공간까지 차지하는 전체 충돌 박스, 작은 창을 표현할 수 없는 좁은 기본 규격 탐색을 기존 원관측으로 반증 실험한다. 새 구조 전용 Gemma 3회 실험은 2개 구조 누락과 1개 부정확한 홈 관측에 그쳐 제품 경로에 채택하지 않았다.
