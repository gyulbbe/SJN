# 15장 사진 재구성 개선 진행 보고 — 2026-09-15

**진행 중이며 품질 완료가 아니다.** 코드·저장·출력 검증 통과와 원사진 재현은 별개다. 원본 15장은 이미 본 개발·회귀 자료이므로 일반화 정확도를 보고하지 않는다.

## 실제로 확인한 원인과 수정

- 관측 일부가 불확실하면 모든 설비를 보류하던 흐름을 명시적인 추정 배치 실험으로 분리했다. 표시되는 물체 수가 늘어도 잘못된 배치를 성공으로 세지 않는다. 일반 사용자 생성은 품질 채택 전까지 기존 경로를 유지한다.
- 실제 모델 응답에서 비데 호스를 큰 샤워기로 분류하거나, 벽면을 수납장으로 분류하거나, 거울 자체를 반사 물체로 제외하거나, 같은 영역을 서로 다른 설비로 중복 분류한 사례가 있었다. 설명과 분류값이 모순인 경우와 설명까지 틀린 시각 오인식을 구분해 보존했다.
- 배치에서는 욕조 상단만 잡힌 관측 영역을 전체 욕조 모형과 비교하는 문제가 남아 있다. 세면볼과 상판을 결합한 모형을 상판 영역만으로 평가하는 오류는 원관측을 보존한 합성 영역으로 수정했다. 이 변경은 실제 추가 6번 사진에서 상판이 더 보이게 했지만 설치·비율 오류를 모두 해결하지 못했다.
- 원본 시점 가설을 표시 과정에서 전체 맞춤 카메라로 교체하던 차이를 확인하고, 선택 가능한 공통 사진 시점과 실제 렌더 스냅샷 경로를 연결했다. 사진 시점 자체는 추정이다. 투영 코드 일치가 물리적 정답이라는 뜻은 아니다.
- 중립 거울의 줄무늬는 그림자를 꺼도 남았다. near 0.1mm / far 10,000,000mm의 깊이 정밀도가 얇은 앞면·본체 간격을 구분하지 못하는 현상으로 확인했다. 실제 보이는 Before/After 모형 범위에서 공통 near/far를 계산해 제거했다. 제품 형상·재질·저장된 시점은 바꾸지 않는다.

## 사진별 공간 크기

사용자가 보이는 비율에 맞춰 현장 크기를 설정해도 된다고 승인했다. room-estimates-20260915.json에 기록한 사진별 폭·깊이·높이는 **테스트 입력이며 실측값이 아니다**. 동일 코드의 2400mm 대조 결과도 보존했다. 방 치수를 모델의 설비 정답이나 사진별 위치 하드코딩에 사용하지 않았다.

## 실제 실행과 이미지 판정

- 현재 15장 관측은 기존 로컬 Qwen3-VL 4B로 실제 69회 호출했다. 최초 체인 67회와 긴 근거의 후속 계약 제한을 고친 뒤 누락된 배치 관계 분석 2회를 이어서 실행한 합계다.
- 15장 대조 렌더는 같은 동결 번들에서 원래 2400mm와 추정 공간 크기를 각각 실행했다. 이 단계는 새 Qwen 관측과 이전 실제 MoGe/DeepLab 기하를 재생한 실험이다. 최종 동일 입력의 전체 앱 실행이라고 표시하지 않는다.
- 원본 / 시작 기준 / 현재 결과의 15행 비교판을 직접 열어 확인했다. 설비가 있는 14장 모두 주요 오류가 남았다. 공사 중인 remote-05는 없는 설비를 추가하지 않았지만 벽 홈·단차 표현이 남아 있다. 공간 크기 조정만으로 인식·배치 오류가 해결되지는 않았다.
- pc-01은 실제 Next API와 production Lab을 통해 별도로 실행했다. DeepLab/Qwen은 새 실행이다. MoGe는 동일 정규화 입력 해시의 pointmap 캐시를 검증해 재사용하고 평면 추출을 다시 했다. 총 63.182초, API 6건 200 응답, 저장·재진입 PNG 픽셀 동일, 원본 보존을 확인했다. 이 결과도 시각 품질은 실패다.
- 이 앱 경계 검사는 실제 앱 모듈/API를 구동하는 자동화 하네스이며, 일반 사용자가 모든 UI 버튼을 클릭한 최종 검증과는 구분한다. 테스트 모듈에 Three가 별도로 포함되어 중복 인스턴스 경고가 있었고 페이지 오류는 없었다.
- pc-01 실행의 주 페이지 JS heap은 40.7MiB, Python RSS는 544.3MiB였다. Python 수치는 캐시 읽기와 평면 추출 범위다. GPU 장치 전체를 10초 간격으로 측정한 최대 6579MiB는 다른 프로세스를 포함하며 모델 전용 최대 메모리가 아니다.

## 검증 상태

앞선 관련 단위·브라우저 계약 검사, 전체 ESLint, 타입 검사, Next 프로덕션 빌드, vinext 빌드가 통과했다. 후속 카메라 수정 후 전체 회귀를 다시 확인 중이다. 첫 전체 Vitest 실행은 147개 파일, 1863개 테스트가 통과했지만 두 테스트 파일의 기존 모의 함수가 새 계약과 맞지 않아 실패했다. 모의 내보내기를 보완한 뒤 재실행하며 실패 기록도 보존한다.

관측 방향/FOV 추가는 일부 설치 벽을 바꾸지만 전체 품질을 개선하지 못해 기본 활성화하지 않는다. 검색 후보의 벽 다양성을 늘리는 실험도 개선 근거가 없어 일반 경로에서 제외했다. 모의 추론을 쓴 브라우저 계약 검사는 모델 인식 품질의 증거가 아니다. 실제 외부 DB 연결·배포는 실행하지 않았다.

## 다음 단계와 승인

현재 모델 범위에서 거울 렌더·합성 영역·배치 연결을 수정했다. 인식 단계의 한계를 분리 검증하려고 Qwen3-VL 8B와 기존 4B에 동일한 사진·프롬프트·스키마·설정을 적용하는 비교 하네스를 준비했다.

후보는 qwen3-vl:8b-instruct-q4_K_M으로 약 6.1GB, Apache 2.0 라이선스이며 기존 Ollama에서 개발용으로만 비교할 예정이다. **새 모델 다운로드·실행은 아직 승인받지 않았고 수행하지 않았다.** 승인되어도 일반 사이트의 모델이나 사용자 브라우저 다운로드 정책을 자동 교체하지 않는다. 더 큰 모델이 실제로 나아지는지는 아직 검증하지 않았다.

공식 자료: [Ollama 8B](https://ollama.com/library/qwen3-vl:8b-instruct-q4_K_M), [Qwen 모델 카드](https://huggingface.co/Qwen/Qwen3-VL-8B-Instruct).

## 근거 위치

기본 산출물은 C:/dev/SJN/test-results/reconstruction-fifteen-rebuild/20260915-start/ 아래다.

- progress.md / iterations/002-appearance-source-view-fifteen/iteration.json: 진행 기록과 실행 출처.
- final-fifteen-comparison-2026-09-15T01-23-02.539Z/: 15행 비교판, 직접 이미지 판정, 후보별 실패 추적.
- actual-app-2026-09-15T01-23-08-422Z/: 실제 앱 원응답, 원본 대조, 저장·재진입 PNG, 시간·메모리 측정 범위.
- observed-direction-render-2026-09-15T01-35-51.326Z/: 3장 카메라 가설 대조.
- assembly-bounds-render-2026-09-15T01-37-13.635Z/: 상판 합성 관측 대조.
- PAIRED-COMPARISON-README.md: 새 모델 비교 준비와 실행 조건.

기존 원본·프로젝트·시안·불변 자재·저장본과 작업 시작 당시의 변경을 보존했다. Git commit/push와 서비스 배포는 하지 않았다.

## v4 최종 회귀 갱신

15장 모두 같은 동결 소스와 실제 관측을 사용해 다시 렌더했다. 이번 렌더 단계의 새 AI 추론은 0회다. 원본·직전 v3·현재 v4 비교판의 15행을 직접 확인했고 창·거울 줄무늬가 제거된 것을 확인했다. 재구성의 주요 인식·배치 오류는 여전히 남는다.

상판 합성 관측 실험에서 추가 6번 사진의 설치가 개선됐던 조건은 2400mm 기본방이었다. 사진에 맞춘 1700×1900×2300mm 시험방에서는 상판이 여전히 오른쪽 벽에 있고 일부 잘린다. 설치 벽 문제가 해결됐다고 보고하지 않는다.

전체 단위 **149개 파일, 1878개 테스트 통과**, 전체 ESLint 통과, Next와 vinext 빌드 통과. 동시에 실행한 빌드가 동일한 생성 타입 파일을 사용해 한 번 실패했으며, vinext의 생성 파일 표기와 코드를 확인한 뒤 Next를 단독으로 재실행해 통과했다. 원래 실패 로그를 보존했다. 배포는 하지 않았다.

최종 근거는 validation-v4.json, iterations/003-assembly-and-depth-precision/iteration.json과 final-v4-regression-2026-09-15T01-48-53.001Z/에 있다. 앞으로 각 개선·테스트 회차마다 원본과 결과 15쌍을 한 장의 클릭 가능한 비교 이미지로 채팅에 남긴다.

## 2026-09-15 렌더 타깃 수정 회차

사진 시점의 화면 비율이 그림자 처리 후 전체 캔버스 비율로 초기화되는 결함을 수정했다. 같은 3D 장면이 가로로 약 2배 늘어나고 양옆이 잘리던 직접 원인이다. 수학 카메라 투영 0px와 미리보기=PNG 검사는 같은 렌더 오류를 공유할 수 있어 이 결함을 놓쳤다. 실제 출력 기준점의 약 120px 오차를 재현하고, 수정 후 두 저장 장면의 각 36개 기준점에서 최대 0.213px 이하로 확인했다.

동일한 v4 장면·설비 계획·카메라·사진별 추정 치수로 15장을 다시 출력하고 root도 모두 직접 봤다. 추가06 타원 거울이 세로 형태로 돌아왔고, 상판·변기·세면대의 과도한 좌우 잘림이 줄었다. 새 AI·solver 계산·모델 다운로드는 각각 0회다. 과거 Blob 해시는 없어 같은 계획으로 복원한 자산의 바이트 동일성을 주장하지 않는다. 원본 15장과 과거 report/scene/PNG 45개 해시는 그대로다.

없는 수납장 생성, 변기 탱크 중복, 거울 누락, 설치 벽·방향 오류와 낮은 벽/홈 표현은 아직 남아 있다. 렌더 개선을 인식 개선 또는 전체 품질 통과로 보고하지 않는다. 작은 위생 샤워는 현재 종류 계약 자체가 호스·수전·샤워 세트를 함께 다루므로 AI 종류 오류와 모형 세분화 부족을 추가로 구분한다.

전체 단위 149개 파일·1878개 테스트, lint, vinext → Next → tsc 순차 검사가 통과했다. 개발 서버 3000을 복원했다. 최종 검증은 validation-viewport-2026-09-15T02-13-10.092Z/validation.json이다. source-viewport-shadow-audit와 docs/reconstruction-source-viewport-20260915.md에 실제 픽셀·저장·비교·모바일 회귀 근거가 있다.

사용자는 회사에서 휴대폰으로 보고 있다. 이번부터 로컬 HTML 링크만 보내지 않고, 원본|현재 결과 15쌍을 3열×5행 한 장으로 채팅에 native image로 직접 표시한다. 이번 원본 이미지는 renderer-only-fifteen-20260915-viewport/review-mobile.png(1800×1950), 채팅 표시 사본은 1500×1625 JPEG다. 고해상도 비교와 이전 결과는 보존한다.

다음은 기존 승인된 4B에 전체 사진만 준 조건과 후보 주변 확대 사진도 준 조건을 비교하는 실험이다. 새 8B 다운로드·실행은 여전히 승인 전이며 하지 않았다.

## 추가07 item_06 원인 판정 정정

추가07의 item_06은 원사진 영역을 확대해 직접 확인한 결과 **거울 아래 긴 벽 선반이 주 대상**이며, 변기 부분은 주변에 조금 포함돼 있었다. 앞선 사람이 작성한 “탱크 부분을 전체 변기로 승격”이라는 설명은 잘못된 AI note를 사진 대신 신뢰한 오판이었다. 확인된 데이터 흐름은 inventory wallShelf → appearance toilet → 별도 바닥 변기 생성이다. 이 사례의 직접 원인은 선반 중심 영역의 오분류이며, typed 부품/전체 연결 설계의 실증 성공 사례로 사용하지 않는다. 원응답과 이전 해석을 보존하고 이 정정을 최신 판단으로 사용한다. root도 context-crop-prepared-2026-09-15T02-28-08.913Z/additional-07/item_06/crop.jpg를 직접 확인했고 사용자에게 정정했다.

## 2026-09-15 기존 4B 확대 입력 대조 완료 — 자동 채택 보류

4사진의 고정 9후보에 전체 사진만 / 전체+후보 주변 확대를 같은 모델·질문·스키마·seed로 실제 18회 완료했다. source run은 context-crop-actual-2026-09-15T02-33-28.910Z의 smoke4개와 같은 계약의 ...02-34-11.426Z 나머지14개다. 원본·production은 변경하지 않았다.

확대 조건은 remote03의 작은 위생 샤워를 shower/hand-spray로 구분했으나 변기에 partOf 연결하는 잘못된 관계도 나왔다. 추가07 선반과 추가09 변기 분류 변화는 전체 사진만 준 조건에서도 나와 확대의 효과로 세지 않는다. 실제 거울의 reflected 오판, 주변 설비 구조 정보의 혼입도 남는다. root도 18개 구조화 응답을 직접 읽었고, 전체 방식의 자동 채택은 보류했다. 15장 재구성 품질 통과가 아니다.

준비 단계의 HTTP 문법 거부 19건은 모델 완료 0건이다. enum 정의를 모델 본문에 전달하지 않은 이전 v4의 18개 유효 JSON은 최종 성능 근거와 분리했다. [Ollama 공식 문서](https://docs.ollama.com/capabilities/structured-outputs)에서도 출력 스키마를 prompt에 함께 제공하는 방법을 안내한다. 본문에 계약 정의를 넣은 v5가 이번 최종 paired18개이며, 여러 revision에서 잘 나온 응답을 섞지 않았다.

실제 모델 응답이 맞아도 관계·필드 적용 검증은 별도로 필요하다. 다음은 확대 사진 안의 정확한 대상 범위와 거울 표면/그 안의 반사 내용 구분을 개선하는 접근이다. 새로운 모델 다운로드 없이 진행 가능한 검증이며 8B 도입은 여전히 승인 전이다.


## 2026-09-15T03:21:35.727Z 진행 갱신

- 휴대폰용 15쌍 한 장은 native 채팅 이미지로 전달 완료. 같은 결과를 반복 게시하지 않는다.
- 대상 테두리 실제8회: 광범위한 영역의 단일 설비 오분류가 유지되어 전체 채택하지 않았다.
- 거울 대상존재 실제8회: 거울 표면 한 필드 재확인 helper 구현 중, 전체 계약은 채택하지 않는다.
- 샤워 별도 실제10회: 실제 importImage.preview→client→local route 입력 hash 확인. 소형 헤드+수전 보류의 좁은 일반 정책만 15장 재구성 비교 준비 중이며, 새10응답 전체가 인식 성공은 아니다.
- 추가04 최소 수동 보정: yaw만 변경은 벽 침범으로 거부. 별도 사본 오른쪽70mm+방향0° 두값을 한 명령으로 적용, undo/redo/저장/재진입 PNG동일. 자동 결과에 섞지 않는다.
- 8B 승인은 여전히 대기. 다운로드·실행 없음. 최종 품질 기준 미달, 작업 계속.

## 2026-09-15 픽셀 단위 보정과 카메라 순위 적용

현재 채택 상태는 v7 camera-only이다. 세면볼 부분 판정에서 정규화 좌표의 비율과 실제 픽셀 비율을 혼동하던 문제를 수정했고, 추가04에서 바뀐 결과를 확인했다. 카메라 점수는 종류 재관측과 충돌한 후보의 이미지 항목을 분리하되, 다른 관측 집단의 일치 검사를 통과할 때만 순위에 반영한다. 추가07의 세면대 위치가 개선됐다. 두 변경 모두 사진 ID·정답 좌표에 의존하지 않는다.

v7-camera-only-default-fifteen-2026-09-15T04-45-38.130Z에서 기본값으로 15장 전체를 재구성했고, 직전 명시적 camera-only의 계획·카메라 및 PNG45개와 동일했다. 새 AI 0회, 원본·관측·소스 해시 유지, 실제 렌더 약35.2초. validation-final-camera-only-2026-09-15T04-44-31.835Z의 전체157파일/2073단위·lint·vinext→Next→tsc 순차 검사가 통과했다. 개발 서버 복원 뒤 Next가 생성한 next-env.d.ts의 두 dev/types 경로 변경만 별도로 확인했으며 이후 tsc도 통과했다.

거울 재확인과 카메라를 결합한 조건은 채팅에 실제 비교 이미지로 보여줬지만 **채택하지 않았다**. 추가07 거울은 오른쪽 벽으로 갔으나 옆 수납장을 카메라 단독 조건보다350mm 낮게 배치하는 퇴행이 있었다. 해당 비교 이미지를 현재 채택 결과로 사용하지 않는다. original/조건별 결과와 거부 사유는 camera-reflection-fifteen-2026-09-15T04-33-44.557Z에 보존했다.

## 2026-09-15 커튼 종류·걸이 모형 구현 중

샤워 커튼을 별도 종류로 추가하고, 바닥 좌표 프레임과 실제 매달림 설치를 분리했다. 봉/레일/지지대 표시 안 함과 양면 불투명한 천, 전체 규격으로 정규화한 실제 걸이 기준점을 지원한다. 표준 모형의 크기와 걸이 높이는 추정 또는 기본값이며 실측으로 표시하지 않는다. 기존 고정 AI inventory/appearance 계약은 바꾸지 않았다.

준비한 고정 후보 재질 계약을 기존4B로10사진13후보에 실제 실행했다. pc04의 같은 가림막을 가리키는 두 후보 모두 physical/fabric-curtain/part/unknown-support로 나왔다. 다른 사진의 벽·거울 답에는 모순이 남아 광범위한 자동 재분류는 채택하지 않는다. 원본·응답·전처리·입력 해시는 divider-material-actual-2026-09-15T05-04-27.561Z에 있다. 이 실행은 직접 호출 실험이며 실제 앱 client/server 전처리 경로의 생산 검증은 따로 진행한다.

커튼은 원후보를 보존하고 검증된 천 재질과 범위에만 적용하는 opt-in 단계로 구현했다. 타입·고정계약·factory·물리 범위·격리 저장·undo/redo·원자료 독립성 단위는 통과했으나 아직 이번 변경의15장 실제 화면 채택 판정은 하지 않았다. 다음으로 동일 코드의 control/trial15장을 비교하고, 휴대폰에서도 보이는 native 채팅 이미지 한 장을 전달한다. 새로운 모델·환경·외부 사진 전송·배포는 하지 않았다.


## 커튼 실제 관측·15장 화면 확인 및 휴대폰 전달 (14:41)

기존 승인4B로 production 재질관측10회(13행), 변경된 pc04 inventory의 새layout1회를53.3초에 실행했다. 이전 Sharp 입력과 현재 브라우저 입력을 구분하고 좋은 응답을 골라 섞지 않았다. 좁은 천 커튼 정책은 pc04 두 중복후보만 변경했고 실물모형 하나로 생성했다. 다른 재질 오인식은 원문에 남기고 적용하지 않았다.

`curtain-controlled-fifteen-2026-09-15T05-34-52.241Z`에서 현재 코드로15장control/trial을 모두 렌더했다. control45PNG는 채택v7과 동일, trial은pc04만 변경되어 다른14장의42PNG는 동일했다. 모든After는 빈 공간이다. 총30장면·각3PNG 생성60.58초, RAM/GPU최고치는 미측정이다. 원본/관측/loaded소스 불변. 새AI는 이 재생 단계0이며 앞의10+1관측이 별도 계보다. 원본 바이트는 동일하지만 일부 브라우저 JPEG 재인코딩 픽셀이 달라, 동일정규화 캐시적중으로 가장하지 않았다.

원본15쌍·pc04확대본을 root와 독립검토자가 직접 확인했다. 천 모형으로 종류개선은 명확하나 새layout이 세면대를300mm뒤로 움직여 전경 정합 한계가 남고, 커튼규격·색·걸이높이는 기본/추정이다. 전체15품질완료는 아니다. 1800×1950 비교이미지를 JPEG68로 용량만 줄여 native채팅 이미지로 전달했다. PC경로링크만 제공하지 않았다.

실제 Properties UI에서 rod→track 편집후 review설치모드가floor로 남아 local저장이 거부되는 오류를 재현했다. 실패번들/스크린샷/validator출력을 보존하고 수정 중이다. plan의geometry설치출처는inferred로, unknown지지방식 문구는미확정으로 바로잡았다. 새Lab개선분석에만 커튼관측을 활성화하며 기존replay는새AI를추가하지 않는다. post-review 핵심203테스트 통과. 마지막 전체2278테스트·lint·vinext·Next·types통과는이후UI/메타데이터수정전소스이므로최종검사를다시구분해기록한다.


## 커튼 단계 최종 검증과 다음 구조 관측 실패 (14:53)

최종source15 재생의90PNG는 앞서 휴대폰에 표시한 실험과완전히같고, 의도한 plan.mounting 출처model→inferred만변경됨을 확인했다. 실제Properties에서지지방식/걸이높이/undo-redo/save-reload를검사하고저장오류를수정했다. 원본16자산·불변12버전·다른설비·빈After보존. 최종164파일2284테스트,lint,vinext→Next→tsc및개발서버복원후tsc통과. source불변. 커튼의좁은변경을채택했지만전체15품질은미달이다.

벽구조 독립관측 첫15호출은모두HTTP/모델/JSON정상이었지만유용한홈·낮은벽을찾지못했다.14장은regions비었고additional06만flat8개와모순필드가나왔다.이를인식성공으로세지않으며현재장면에적용하지않았다.정상토큰종료라실행한도문제는아니며,동시요구가많은관측설계의억제가능성은가설로남긴다.기존MoGe가보존한dense geometry.npz가발견되어15입력SHA와대조중이다.새모델/설치/추론없이깊이근거를점검한후다음가설을결정한다.벽홈geometry프로토타입은작성된테스트형상의CPU검증일뿐실제사진성공과분리했다.

## 15:10 KST — 기존 깊이 데이터 검증과 다음 가설

15장 모두 원본 SHA·정규화 입력·MoGe revision·geometry SHA가 연결된 기존 dense 캐시를 확인했다. CPU 검사 4.01초, 새 AI 0회이며 원본과 캐시는 변경하지 않았다. 모든 mask가 true라 신뢰도처럼 사용할 수 없다. remote04 낮은 벽과 remote05 벽 후퇴의 일부 경계가 법선 데이터에 남아 있어, 같은 설정으로 15장의 연결 평면·깊이 불연속 후보를 만드는 실험을 시작했다. 아직 자동 구조 인식 성공이나 채택은 아니다.

별도 카메라 감사에서 11장의 선택 배치 카메라와 MoGe 화각이 다르므로, 픽셀 후보를 곧바로 기존 카메라로 역투영하면 안 된다. MoGe 거리도 추정 m 단위이며 실측이 아니다. 먼저 자동 후보의 위치와 범위를 시각 검증하고, 필요하면 기존 4B로 고정된 영역의 최소 분류만 확인한다. 새 모델·환경·생산 소스 변경은 없다. remote05는 바닥에 닿는 벽 후퇴일 가능성이 있어 닫힌 홈 모형의 아래턱을 그대로 만들지 않기로 했다.
