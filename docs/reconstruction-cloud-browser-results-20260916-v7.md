# 2026-09-16 Cloudflare + 브라우저 재구성 v7 실제 검증

현재 목표는 **미완료**다. 동일 v7 소스에서 15장 모두 실제 분석·재구성·PNG까지 완료했지만 고정 평가 기준의 핵심 통과는 remote-03, additional-04 두 장이며, 나머지 13장에는 주요 결함이 남았다. 실행 성공을 품질 성공으로 판정하지 않았다. v7 전체 변경 묶음을 최종 품질 개선 완료로 채택하지 않는다.

## 실행 근거

- 결과: `test-results/reconstruction-fifteen-rebuild/2026-09-16T02-41-56-428Z-cloud-gemma/full15-ui-v7-01`
- 소스 SHA256: `136341c7e9c833e600b2fb94e42f08f08f965033adddfe028fcf77f0ab90b5b6` (296파일, 실행 전후 동일)
- 실제 실행: 2026-09-16 05:22:26–05:34:55 UTC, 약 12분 29초
- HTTP POST 86, 알려진 바인딩 호출 86, 호출 수 불명 0, Cloudflare 단계 캐시 0. 보고된 입력 115,798 / 출력 16,292 토큰. 계정 청구·무료 잔여량 측정은 아니다.
- DeepLab는 완료된 동일 사진 분할 재사용, MoGe는 브라우저에서 실행. 사진별 단계·입력·원응답·시간·SHA는 각 attempt와 execution-summary.json에 있다.
- 개발 PC(7800X3D / RTX4070Ti / 32GB, Chrome152)에서 일부 단위·모의 UI 검사와 실행 시간이 겹쳤다. 독립 성능 벤치마크로 일반화하지 않는다. 정확한 RAM/VRAM 최고 사용량은 미측정이다.
- 모든 원본 사진·이전 프로젝트·과거 실행을 보존했다. 별도 새 모델/실행 환경 설치, 유료 대체, 커밋·푸시·배포는 하지 않았다.

## 시각 평가

15장 원본·v6·v7 PNG와 원응답을 직접 비교했다. 모바일 전체 비교는 `full15-ui-v7-01/mobile-original-current.jpg`, 원격5장 판정은 quality-review-v7-remote.json, PC4+추가6은 quality-review-v7-pc-additional.json에 있다. 표준 공통 카메라와 원사진 카메라 차이를 인식 개선/실패 자체로 취급하지 않았다.

- 좋아진 부분: pc01 욕조·유리, pc04 아치 거울, remote01 상부 창, remote02 유리, 추가05 유리·선반·랙, 추가09 거울·샤워.
- 주요 회귀: 추가06에서 볼+상판 조합 두 개 생성, 추가07에서 거울장 대신 허위 창 두 개 생성, remote04 유리를 반사된 샤워로 바꾸어 제거.
- 계속 남은 문제: 거울장·선반·창의 후속 관측 오분류, 공사 중 배관을 설치된 샤워로 오인, 낮은 벽·턱 누락, 설치 벽/앞뒤/방향/크기 관계 오류.
- 67개 appearance 후보 중 known→unknown 5개, cross-family 4개, physical→reflected 5개가 있으며 서로 중첩된다. 기존 후보도 중복·오분류일 수 있어 이전 라벨을 무조건 유지하는 정책은 채택하지 않았다.

## 코드 및 일반 화면 검사

- 단위 192파일 / 2,881개 PASS. 최초 실패 한 건은 두 번째 비동기 구독 완료를 기다리지 않은 테스트 race였으며 정확한 구독 barrier와 지연 경계 회귀로 수정했다. 관련 반복 60/60 PASS. 제품 취소 동작은 변경하지 않았다.
- lint, 타입 검사, Next.js 빌드, vinext 빌드 PASS.
- 모의 브라우저 12개 PASS. 이 검사는 실제 AI/모델 다운로드 0회이며 실제 품질 검증과 별개다.
- 일반 UI 실제 검증 `normal-ui-v7-01`: 비교 공간 생성→빈 After 확인→저장/재진입→타일 등록·After 편집→Before 재분석→After 보존→undo/redo→새로고침→PNG 다운로드 PASS. 실제 검증된 앱 캐시를 사용했으며 새 Cloudflare POST 0회다.
- 선택 있음/없음 PNG 픽셀이 동일하여 편집 표시가 출력에 섞이지 않는 것을 확인했다.
- 일반 UI 테스트 결과의 performanceRenderer 문자열은 과거 v5 스크립트의 하드코딩이 남아 있었다. 원본 결과를 보존하고 renderer-metadata-correction.json에 정정했다. 측정된 편집기 legacy canvas 1개와 실제 Lab report의 legacy-front가 일치한다.
- 대량 15장 실행에서 JSON 버튼 다운로드는 명시적으로 생략하고 실제 UI JSON을 그대로 보관했다. 별도의 v6 전체 앱 캐시 실행에서 JSON 다운로드/바이트 동일성/브라우저 생존은 통과했으나, 이전 fresh-GPU 후 Chrome crash의 원인은 해결됐다고 주장하지 않는다.

## 다음 검증된 가설

- appearance-scope-probe-01: 같은 Gemma·사진·보드·schema·설정에 공통 대상 범위 문구만 변경, 실제2회. remote01 선반2개와 pc04 창이 지원 종류/실물로 복구됐다. 일부 shape/note는 바뀌었고 최종 배치 검증은 아직 아니다.
- rough-in-shower-probe-01: 같은 기존 모델로 full/crop 기반 설치 상태 실제2회. remote05 미설치 배관과 remote01 실제 샤워 구분을 확인했다. 강한 음성만 후보 보존+미배치 확인으로 연결하는 구현을 준비한다.
- appearance-conflict-target-probe-01: 후속 단계의 종류·반사 충돌에 고정 단일 대상 재확인이 도움이 되는지 별도 검증한다.
- 단일 상판/볼의 부품·전체 계약과 상대 배치 제약은 추가 개선이 필요하다.

위 후속 작업은 별도 revision과 원문·입력 해시를 유지하고, 적용한 뒤 좁은 실제 검증과 15장 회귀를 다시 수행한다. 현재의 미해결 품질을 완료로 보고하지 않는다. Gateway 대시보드 로그 권한과 실제 저사양/모바일 기기의 메모리 검증도 여전히 별도 미확인 항목이다.
