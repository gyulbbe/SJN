# 사진 재구성 현재 상태

## 2026-09-14 자동 배치 원인 수정·간단 확인 최신 인계

요청한 `before-reconstruction-placement-quality.md`의 원인 조사·구현·같은 실제 관측 재생을 수행했다. [통합 결과](reconstruction-placement-quality-results-20260914.md)를 최신 근거로 읽는다. 텍스처용 바닥 quad/임시 깊이를 방 전체 좌표로 승격하던 오류, 근거 없는 벽 방향 추정과 부분 벽 폭 계산을 수정했다. 기존 Lab에 후보 7단계 근거를 연결하고, Before 보류 후보는 벽·방향 선택과 그림 클릭으로 추가할 수 있다. 위치 제안 자체가 없던 벽걸이 세면대도 같은 흐름으로 확인한다. 사진·원관측·기존 변경·불변 버전은 보존했다.

**자동 시각 재현은 개선되지 않았다.** 동일 user01~04/prospective03의 과거 v6 관측을 재생한 모형 수는 **0/0/0/2/0 → 0/0/0/1/0**이다. user04 하부장도 실제 바닥 대응이 없어 확인 대상으로 남았다. [자동 전후 비교](../test-results/reconstruction-placement-quality-20260914/comparison.html)와 [사용자 확인·수동 추가 결과](../test-results/reconstruction-placement-quality-20260914/user-confirmed-comparison.html)를 분리했다. 후자의 6/3/4/4개는 사용자 입력이며 자동 인식 성공이 아니다.

최종 생산 178개 파일 해시가 검사 전후 같고, **110파일 1,388개 단위**, lint·타입·Next.js→vinext 순차 빌드가 모두 종료 0이다. 마지막 UI 수정 후 실제 Next E2E 8개, 실제 user03 그림 배치·undo/redo·저장/재진입, user01 위치 제안 없는 후보 추가를 통과했다. 기존 Lab 다중 사진 전체 실행 두 버튼·순차 처리·진단·전체 JSON 다운로드도 2장 저장 관측/PNG 재생으로 통과했다. 새 AI 추론·모델 변경·사진 외부 전송·배포는 없었다. 모델/전체 RAM·VRAM peak는 측정하지 않았으며 메인 페이지 처리 후 JS heap만 기록했다. source camera는 계속 미확정이다. 이번 출력은 baseline v10/candidate v13이고 관측 캐시 v7은 그대로다. 과거 v6 기록은 명시적인 검증 재생 자료로만 사용한다.

이 아래의 이전 인계는 해당 시점의 결과로 보존한다.

---

## 2026-09-14 후속 품질 개선 최신 인계

색상 관측/기본색/사용자색 분리, 세면대·하부장 위 설치 참고 추천, 볼과 별도의 기둥 단면, 요청 위치·규격을 강제로 줄이거나 옮기지 않는 preserve 배치와 보류 후보 수정 흐름을 구현·검증했다. 최신 근거는 [후속 품질 결과](reconstruction-next-quality-results-20260914.md), [엄격 배치·관측 캐시](reconstruction-strict-placement-results-20260914.md), [실제 보류 후보 Review 검증](reconstruction-strict-review-verification-20260914.md)을 따른다.

**원사진의 자동 재현은 아직 충분하지 않다.** 동일 실제 관측의 최종 run-02-provenance 재생에서 사용자 수정 없는 배치는 user01~04/prospective03 순으로 **0/0/0/2/0개**다. 이전의 2/1/2/3/4개 중 방 범위를 침범하던 10개를 자동 이동·축소하지 않고 추가 보류했다. 이는 오류를 숨기지 않게 된 결과이지 자동 배치 품질 향상이 아니다. 사용자가 확인한 네 사진 결과 **6/3/4/4개**는 따로 유지되며, strict 변경 전후 PNG SHA256도 같다. [5사진 원본·이전·이번 비교](../test-results/reconstruction-next-quality-20260914/comparison.html)는 15이미지/5구간을 데스크톱과 모바일에서 확인했다.

8개 실제 Next E2E는 strict 적용 후, 마지막 Review 입력/출처 및 캐시 수정 전 결과다. 그 이후 생산 코드의 전체 **107파일 1,336개** 테스트와 lint/type·Next.js→vinext 순차 빌드가 종료 0이며, 추가 실제 Review UI는 user03 원관측 Before 0개에서 사용자 확인 2개를 추가하고 저장·새로고침까지 통과했다. 1e9/NaN 입력은 문서·revision을 바꾸지 않으며, 바꾸지 않은 규격의 default 출처와 색 근거를 보존한다. 이 새 UI 테스트의 scoped lint/type도 종료 0이다. 마지막 새 Review 테스트까지 포함한 전체 lint → typecheck 재확인도 루트 handle 21794에서 종료 0이며, 검사한 생산 160개 파일 해시는 최종 코드와 모두 같다.

새 AI 추론과 보존 관측 재생, 규칙 제안과 사용자 입력을 구분한다. 원사진 카메라는 5사례 모두 미확정이며 source 재투영 오차는 not-evaluable이다. RAM/VRAM peak는 새로 측정하지 않았다. DeepLab·Qwen·TripoSR/배경 제거의 역할을 교체하거나 유료 API·사진 외부 전송·실제 서버 배포를 추가하지 않았다. 최종 관측 캐시 v7과 출력 baseline v9/candidate v12를 구분하며, 과거 v6 관측은 명시적 재생 자료로만 사용한다.

다음 개선 요청은 [자동 배치·재현 품질 개선 프롬프트](prompts/before-reconstruction-placement-quality.md)에 정리했다.

이 아래의 이전 인계와 진행 기록은 당시 증거로 보존한다. 과거 자동 배치 수·revision·1,241개 검사와 그때의 완료 범위를 현재 후속 목표의 수치로 인용하지 않는다.

---


## 2026-09-14 최종 인계

요청한 관측/설치/표준 모형 연결 개선과 사용자 보정·저장·검증 범위를 완료했다. 최종 전역100파일1,241테스트, lint·타입·Next.js·vinext 검사와 네 사진 프로젝트 E2E가 통과했다. 위치 참고 기록의 실제 저장/재진입도 확인했다.

최신 판단·원인·코드·실제 모델 개수와 시간·사진별 비교·제한사항은 [최종 인계 문서](reconstruction-completion-20260914.md)를 기준으로 읽는다. 자동 공간 추정과 색상·세부 형태 한계는 남아 있으며 후보 분석기는 기본으로 채택하지 않았다. [후속 프롬프트](prompts/before-reconstruction-next-quality.md).

아래 내용은 **최종 위치 참고/필드 수정 이전의 2026-09-13 진행 기록**으로 보존한다. 아래의 ‘진행 중’, 옛 검사 수치와 미검증 항목은 당시 상태이며 최신 결론으로 인용하지 않는다. 현재 코드 revision은 baseline v7, candidate v10이고 원래 실제 실행 보고서의 revision을 소급 변경하지 않았다.

---

## 보존된 이전 진행 기록

# 사진 재구성 현재 상태

2026-09-13. **원래 사진→Before 개선 목표는 진행 중이며 전체 완료가 아니다.** 사진 속 설비가 알아보이는 표준 공간까지 최소한의 확인으로 완성하는 것이 목표다. 자동 관측의 정상 종료, 단위 검사 통과, 사용자 역할로 만든 결과를 전체 자동 재현 성공으로 바꾸어 보고하지 않는다. 개선 후보를 기본 분석기로 채택할 근거도 아직 부족하다.

이 문서는 마지막 완료된 통합 검사 `reconstruction-independent-support-final-20260913/checks`와 아래의 실제 결과를 기준으로 정리한다. **카메라와 독립적인 설치 판단 및 기둥 윤곽 하단의 연결은 실제 후보/Lab 경로에 반영했다.** 자동 배치 전체 품질과 카메라 가설의 자동 채택은 미완료다. 기존 날짜별 문서·보고서·소스 스냅샷은 당시 결과로 보존하며, 이전 문서의 미연결/검사 수치와 이 문서의 최신 상태를 혼합하지 않는다.

## 지금 구현되어 있는 흐름

| 영역 | 현재 사용 가능한 동작과 범위 |
|---|---|
| 관측과 배치 분리 | 기존 DeepLab 관측, Qwen 후보 원문, 중복/충돌·부품·반사 정리, 설치 판단, 기하, 모형 검증을 분리한다. 문제 후보는 이유와 함께 남기며 JSON 손상은 성공으로 자동 수리하지 않는다. 유리 뒤 욕조를 중첩만으로 지우거나 상판 세면볼을 바닥 설비로 중복 생성하지 않는다. |
| 표준 모형 | 사각/곡면 볼과 벽걸이/기둥/하부장 지지를 분리한다. 1/2볼과 열린/닫힌 변기, 중립 거울/거울장, 반투명 유리·선반을 표현한다. 기둥형 사각 볼은 실제 semantic/RGB 윤곽 근거를 사용할 수 있다. 볼 수·뚜껑은 모델 관측, 규칙 추론, 기본값, 사용자 확인을 구분한다. 넓은 하부장을 2볼 관측으로 기록하지 않는다. |
| 독립 설치·지지점 | 카메라가 보류돼도 동일 설비의 지지 방식을 확인 목록에 유지한다. 검증한 기둥 윤곽 하단은 모델 anchor와 별도 출처로 사용하며, 기둥형의 볼 bbox 하단을 바닥으로 투영하는 경로는 보류한다. 위치만 보정할 때 이미 판단한 지지 방식은 유지한다. [구현·검증](reconstruction-independent-support-results-20260913.md). |
| 기본 규격 | 바닥 설비 외곽을 뒤 벽의 homography로 환산해 제품 크기를 과장하던 오류를 제거했다. 검증된 체적 규격이 없는 baseline은 종류별 기본값과 개별 폭/높이/깊이 출처를 유지한다. 색·볼 형태·볼 수·뚜껑은 덮어쓰지 않는다. 벽면 모형의 기존 근거 있는 크기 추정은 유지한다. |
| 간단한 보정 | Lab에서 종류·설치 방식·벽·형태·개수·실체 여부와 위치/높이/규격을 확인하고 AI 재실행 없이 별도 결과를 만든다. 사진 번호와 설비 목록을 연결하고 반사·상판 부품 관계를 수정할 수 있다. 벽 기준 거리 모드에서도 그림 클릭/드래그·키보드·터치로 이동하며 선택한 벽 방향을 유지한다. |
| 유리 지지 | 사용자 확인 shower-curb는 높이·폭·깊이와 출처를 저장하고 실제 표준 턱을 그린다. bath-rim은 기존 욕조 후보 또는 배치한 표준 욕조를 명시적으로 선택해 테두리·거리를 연결한다. 후보 ID는 실제 생성 fixture ID로 변환한다. 부모 누락/보류/제외/종류 변경 시 유리를 이유·입력과 함께 보류한다. 독립 높이만 있는 과거 자료에 가짜 욕조를 만들지 않는다. |
| 결과와 실제 프로젝트 | Lab 자동 결과와 사용자 교정·원문·스냅샷을 별도로 보관한다. **이 결과로 공간 만들기**에서 명시적으로 로컬 프로젝트를 저장하며, 보정 결과는 Before·같은 방의 빈 After로 시작한다. 실제 Before에서도 욕조 부모 이동·삭제/undo·재진입·복제 참조를 유지한다. Lab 세션 목록은 페이지 메모리이므로 새로고침하면 비워지지만 저장 프로젝트는 남는다. |
| 실패·시간·재사용 | 분석 중/일부 또는 전체 보류/실패/취소를 구분한다. 원출력·정리 후·실제 배치·확인 필요 수와 단계별 원문·종료 사유·가능한 토큰/시간을 남긴다. 호환 사진/치수/모델/설정 결과는 재사용하고 새 단계 시간과 과거 시간을 분리한다. 취소·늦은 결과·자원 정리와 사용자 입력 복원을 처리한다. |

기본 경로는 DeepLab이며 Qwen3-VL4B는 연결된 개발용 로컬 엔진의 비교 후보다. 새 대형 모델을 사용자 브라우저에 기본 다운로드하지 않는다. 현재 완료된 규격 수정의 캐시 revision은 baseline `deeplab-observed-v7-floor-size-default-2026-09-13`, candidate `structured-scene-v9-floor-size-default-1`이다. Qwen prompt8·`fixture-inventory-v2`·모델 설정은 이 규격 수정에서 변경하지 않았다. 원 실행 보고서의 revision은 소급 변경하지 않는다.

구현별 근거: [기본 바닥 규격](reconstruction-baseline-floor-size-20260913.md), [벽 기준 그림 이동](reconstruction-wall-drag.md), [Lab 욕조 연결](reconstruction-lab-bath-rim.md), [Before 욕조 연결](reconstruction-bath-rim.md), [샤워 턱](reconstruction-shower-curb.md), [로컬 프로젝트 저장](reconstruction-support-project-results-20260913.md). 이전 [Lab 안내](reconstruction-lab.md)와 [파이프라인 설명](reconstruction-pipeline.md)은 이전 실행·미연결 시점의 기록을 포함한다.

## 네 개발 사진: 자동 결과와 사용자 결과를 구분

네 사진은 여러 개선에 사용한 **개발 회귀 사례**다. baseline의 마지막 새 DeepLab 실행은 `after-installation-shape-final`의 v5 보고서다. 최신 v7 규격 수정은 이 실제 rawReview를 동일하게 넣은 **새 추론 없는 전후 생성/렌더 재생**으로 검증했다. 과거 보고서를 현재 모델이 다시 분석한 결과로 부르지 않는다.

| 사진 | 실제 baseline 관측·현재 규격 재생 | 별도 사용자 역할 보정으로 확인한 결과 |
|---|---|---|
| user-01 / TEST 화장실.jpg | 변기·욕조 2개 배치. 세면대는 검출 후 wall 방식 추정까지 했으나 실제 설치 벽/높이 미확정으로 보류. 거울도 보류. 유리·거울장·벽 선반은 baseline의 별도 종류 관측으로 해결되지 않음. | 6개: 변기·벽걸이 세면대·욕조·거울장·욕조 연결 유리·선반. 거울장은 기존 사용자 설명에 따라 확인했고 실제 선반은 수동 추가. 모델 검출 성공6개가 아님. |
| user-02 / TEST 화장실2.jpg | 변기 1개 배치. 둥근 세면볼 윤곽은 실제 관측됐지만 설치 벽이 없어 세면대·거울 보류. 열린 뚜껑을 자동 판단할 조합 근거가 부족해 closed/default. | 3개: 둥근 벽걸이 세면대·거울·열린 변기. 설치와 잘못된 반사·뚜껑을 사용자 확인. 문/문틀 등 모든 물체까지 완성한 결과는 아님. |
| user-03 / test 화장실3.jpg | 사각 기둥 세면대와 열린 변기 2개 배치. 사각은 semantic-rgb-contour, 기둥과 열림은 실제 구성 근거에 따른 추론. 접점/기본 구도는 여전히 근사이며 거울·유리까지 자동 재현하지 못함. | 4개: 변기·기둥 세면대·거울·턱 위 유리. 턱900×160×100mm와 위치는 기능 시험의 사용자 입력이며 사진 실측값이 아님. 앞 변기가 기둥 일부를 가리는 한계가 있음. |
| user-04 / images.jpg | 창·1볼 기본 하부장·변기 3개 배치. 실제 사진은 2볼이나 baseline에는 개수 관측 근거가 없어 1/default. 거울은 검출 후 벽 미확정으로 보류. 변기 510×990×870/inferred를 400×750×680/default로 수정. | 4개: 창·거울·열린 변기·둥근 2볼 하부장. 2볼은 후보 모델 관측을 유지했고 둥근 볼과 설치/위치/뚜껑은 사용자 확인. 하부장 실제 길이·창 분할·카메라까지 정답임을 입증하지 않음. |

규격 전후 재생에서 배치 수 **2/1/2/3**, 보류 상태, 색·모형 형태·볼 수·뚜껑은 유지됐다. user01~03 PNG의 RGBA 차이는0이며 user04만 변기와 그 그림자/가림이 바뀌었다. 깊이 변경에 따라 기존 `fitReconstructionFootprint`가 user04 변기 중심 v를 .18125→.1416667로 조정했다. 정규화 규칙은 변경하지 않았고 정확한 사진 접점 복원으로 주장하지 않는다. [전후 비교](../test-results/reconstruction-baseline-floor-size-20260913/comparison.html) · [실제 검증](../test-results/reconstruction-baseline-floor-size-20260913/verification.json).

네 사진의 [연속 UI 검사](reconstruction-lab-four-photos-results-20260913.md)는 최초 실제 관측만 재생한 뒤 생산 Lab 보정→명시 저장→실제 Next `/projects/{id}`→새로고침→After 타일 undo/redo→독립 시안 복사→4096px 비교 PNG까지 통과했다. user01 run-02와 user02/03/04 run-03은 당시 동일한11개 소스 해시로 검증했다. 이후 벽 드래그·바닥 규격 코드가 바뀌었으므로 이4장 성공을 최종 소스 전체에 대한 새4장 시험으로 부르지 않는다. 최초 user01 navigation timeout과 대기 제한만 늘린 재검증 이력도 보존했다.

후속 최종 user04 검사는 `reconstruction-wall-floor-final-20260913/e2e`에서 **passed**를 직접 확인했다. candidate v9 교정에서 설비4개, 같은 방/빈 After·원 보고서·새로고침 보존, 타일 undo/redo, 독립 시안과4096×1366 PNG가 확인됐다. AI 요청·외부 요청·페이지 예외는0이다. 전체 자동화63.290초 중 실제 교정 생성은2.809초이며 추가 모델 시간은0이다. 사람의 최소 보정 시간이나 인식 성능 수치가 아니다. [최종 user04 검증](../test-results/reconstruction-wall-floor-final-20260913/e2e/reconstruction-lab-four-ph-e7e9b--저장-reload-undo-redo-시안-PNG/verification.json).

## 코드와 실제 실험 결과만 있고 일반 UI에는 없는 것

| 실험 | 실제 확인한 결과 | 아직 적용하지 않은 범위 |
|---|---|---|
| MoGe/표면 기반 촬영 기하 | 실제 로컬 MoGe와 기존 DeepLab/Qwen 자료를 재생한 개발 실험에서 user04 창·거울·변기3개. 나머지3사진은0개. 같은 후보의 기하 없는 결과0/0/0/1과 비교했으며 새 Qwen 추론은 아님. | 일반 Lab에 깊이 모델 실행/다운로드를 연결하거나 기본 엔진으로 채택하지 않음. 표준 Before/After 카메라는 그대로. [기하 결과](reconstruction-depth-installation-results-20260913.md). |
| 광학 영역 제외·평면 조각/카메라 가설 | 유리·거울 등의 실제 관측을 제외해 user03 벽 후보6→3, 두 미선택 가설의 투영 민감도 최대2.873px. 그러나 공동 평면 재적합 실패로 strict 카메라 보류, 두 가설 후단 배치도0개. user01은 반대로 strict 카메라가 더 보수적으로 보류됨. | 숫자가 작다는 이유로 카메라를 자동 선택하지 않음. 이 표의 평면 실험은 당시 진단이며, 후속 접점 연결은 별도 구현 문서를 따른다. 카메라 가설은 계속 미선택이다. [평면·가설 실험](reconstruction-plane-evidence-results-20260913.md). |
| 하부장 표면·기본 규격 적합 | user04 하부장 후보 bbox 하단 중앙이 몸체 중심이 아닌 바닥 픽셀임을 실제 분류로 확인. 고정1200×550mm 규격의 두 축 적합은 모두 보류. | 표면 기반 자동 자세 적용0개. 가까운 벽을 제품 뒤쪽으로 확정하지 않음. [고정 자세 실험](reconstruction-product-pose-experiment-20260913.md). |
| 관측 규격·자세 대안 | 실제 같은 cabinet 몸체 두 면으로 사용자 검토용 proxy4개를 반환. 기존 고정 오차로 비교하면 포함률94.9878%로97% 기준 미달이며, 커진 상대 허용오차의 효과를 별도 기록. | `review-required` 대안 생성 함수/재생까지다. Lab 표면 공급·사용자 수락·관측 출처를 유지한 프로젝트 저장은 미연결. 자동 성공이나 실측 크기가 아님. [대안 실험](reconstruction-observed-product-proposal-20260913.md). |

이 실험들의 입력은 이미 개발에 사용한 사진과 저장 관측이다. 실제 모델 실행, 저장 관측 후처리 재생, 사용자 선택을 서로 대체하지 않는다. 후속 지지점 연결은 [설치 분리 전후 실제 재생](reconstruction-camera-support-separation-20260913.md)에서 확인했다. 일반0/0/0/1 및 깊이실험0/0/0/3 배치 계획은 그대로이며 원관측·카메라도 불변이다. user03 기둥 하단은 연결됐지만 두 미선택 가설의 기본 방향 모형은 벽 침범으로 계속 보류된다.

## 별도 평가와 남은 원래 목표

[별도 평가3장](reconstruction-evaluation-split-20260913.md)은 결과 전에 기준·입력/코드 해시를 고정하고 cached DeepLab/Qwen을 각 1회 실행했다. 원사진 선정에는 이전에 노출됐지만 당시 개발 결과 사용 기록은 없던 작은 prospective slice이며 완전 blind set이 아니다. 결과를 본 현재 이후의 사용은 회귀로 구분한다.

| 별도 사례 | baseline 원출력/정리/배치 | 후보 원출력/정리/배치 | 확인된 한계 |
|---|---:|---:|---|
| prospective-01 | 13/13/1 | 4/4/0 | 쪼그려 쓰는 변기를 탱크 좌변기로 표현하고 다른 설비 누락 |
| prospective-02 | 16/16/0 | 5/4/0 | 양쪽 최종 공간의 설비0개 |
| prospective-03 | 13/13/4 | 8/7/0 | 큰 욕조와 변기2개 오표현; ignore영역 때문에 단일 오탐 지표가 이를 숨길 수 있음 |

후보9/9 bbox 대응은 일부 알려진 종류의 위치 대응일 뿐 형태·설치·재구성 성공률이 아니다. 이3장에서는 실제 간단한 사용자 보정을 거쳐 알아볼 수 있는 Before까지 가는 경로를 검증하지 않았다. 마지막 바닥 기본 규격 변경도 이 평가의 새 모델 실행으로 소급하지 않는다. 과거36장과 bath-04/08/12/16·20/22/27/31 역시 버전별 회귀 기록이지 최신 코드의 미노출 평가가 아니다.

남은 원래 목표는 **같은 실물의 실제 지지 접점·설치 벽·형태/기본 규격·촬영 기하를 연결하고, 자동으로 어려운 경우에도 적은 확인으로 알아볼 수 있는 전체 Before에 도달하는 것**이다. 현재는 특히 벽걸이 세면대의 벽·높이, 잘린 변기, 유리 뒤 벽/가림, 거울 반사, 하부장 중심·앞뒤·길이가 어렵다. 구현된 사용자 보정으로 핵심 설비를 표현할 수 있어도 원사진 구도·제품 외곽·상대 높이를 모두 맞춘 것은 아니다. 인간의 최소 입력 횟수·완료 시간은 아직 측정하지 않았다.

원래 지원 범위 밖의 새 설비 형상이나 완전 자동 생성만을 임의의 추가 완료 조건으로 만들지 않는다. 반대로 분석 정상 종료·목록 보존만으로 알아볼 수 있는 공간 재현을 완료했다고 하지 않는다.

## 마지막 완료된 통합 검사와 보존 조건

직접 확인한 최신 로그는 [독립 지지점 checks.json](../test-results/reconstruction-independent-support-final-20260913/checks/checks.json)이다. 이후 추가한 실제 Lab 브라우저 테스트까지 포함한 타입 검사도 exit0이다.

| 검사 | 실제 결과 |
|---|---|
| ESLint | exit0, 17.344초 |
| 타입 검사 | exit0, 5.116초 |
| Vitest | **98개 파일, 1,208개 통과**, 명령9.119초 |
| Next.js build | exit0, 53.223초 |
| vinext build | Next 뒤 순차 실행, exit0, 20.481초 |

첫 통합 시도는 `ObservedProductProposal.proposalId` 타입 누락으로 typecheck exit2였다. 타입을 수정한 뒤 run-02에서 전부 통과했다. [이전 실패 로그](../test-results/reconstruction-wall-floor-final-20260913/typecheck.log)를 남겼으며 소스 검사 실패를 모델 실행 실패와 혼동하지 않는다. 이번 접점 통합에서는 사진 지문이 없던 기존 기둥 재사용 시험을 새 계약에 맞췄고, 잘못된 reflection enum을 넣는 음성 시험은 명시적 형변환으로 런타임 잘못값을 유지했다. 새 통합 시험의 설치 충돌 입력도 유일 대응이 먼저 성립하도록 고친 뒤 통과했다. 각 실패를 자동 인식 실패로 합산하지 않는다. 세부 scoped 테스트 수는 서로 중복되므로 합쳐 전체 테스트 수로 쓰지 않는다.

기존 원사진·과거 문서·불변 자재 버전과 사용자 입력 출처를 유지한다. 사진 기반 Before/빈 After, 공통 치수·카메라, 기본 벽·바닥 보호, 시안·저장·undo/redo·비교·다운로드를 보존한다. 임의 제품 가격/수량을 추가하거나 TripoSR·제품 배경 제거 모델을 교체하지 않았다. 개인 사진/원문/평가 결과는 Git에서 제외한 로컬 자료다. 실제 Supabase 연결·외부 배포·사진 외부 전송은 수행하지 않았다. 새 메모리 측정을 이 상태 문서에서 만들지 않으며 페이지 heap·Worker/WASM·GPU·모델 할당을 같은 메모리 수치로 섞지 않는다.

후속 user03 전체 실제 앱 E2E도 33.6초에 통과했다. 초기 실제 모델 보고서만 재생하고 현재 보정·프로젝트 저장·새로고침·After undo/redo·시안·PNG를 실행한 기능 검사다. [결과](../test-results/reconstruction-independent-support-final-20260913/e2e/reconstruction-lab-four-ph-f7fb6--저장-reload-undo-redo-시안-PNG/verification.json). 두 미선택 카메라에서 사용자가 왼쪽 벽을 확인했다고 가정한 [별도 PNG 비교](../test-results/reconstruction-support-contact-wall-render-20260913/renders/comparison.html)는 기본 방향0개→세면대1개지만, 카메라 자동 선택이나 사진 전체 재현 성공이 아니다.


실제 Lab의 새 지지점 안내·위치만 보정 경로도 [브라우저 검사](../test-results/reconstruction-lab-support-contact-20260913/verification.json)에서 통과했다. 보존된 user03 모델의 floor/pedestal은 원래 알려져 있으므로 독립 설치 배너가0개인 것이 맞고, unknown→floor 개선으로 세지 않는다. 새 지지점 안내는 accepted이며 u/v만 입력한 뒤 사각 기둥형1개가 생성됐다. 600×800×480mm는 default 출처를 유지했고 원문·model/automaticUnderstanding·미확정 wall은 그대로다. PNG 다운로드와 미리보기 SHA가 같고 새 AI·외부 통신·페이지 예외는0이다. 표시 하네스는 첫 현재 관측 재생을 기본 열에 보여주기 위해 복사본의 correctionOfRunId만 제거했으며 production 결과 원본은 별도 파일로 보존했다. 이 어댑터의 실행 분류를 실제 AI 호출로 해석하지 않는다. 마지막 테스트 수정까지 포함한 타입 검사도 exit0이다.
